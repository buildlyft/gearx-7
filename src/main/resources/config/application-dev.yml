# ===================================================================
# Spring Boot configuration for the "dev" profile.
#
# This configuration overrides the application.yml file.
#
# More information on profiles: https://www.jhipster.tech/profiles/
# More information on configuration properties: https://www.jhipster.tech/common-application-properties/
# ===================================================================

# ===================================================================
# Standard Spring Boot properties.
# Full reference is available at:
# http://docs.spring.io/spring-boot/docs/current/reference/html/common-application-properties.html
# ===================================================================

logging:
  level:
    ROOT: DEBUG
    tech.jhipster: DEBUG
    org.hibernate.SQL: DEBUG
    com.gearx7.app: DEBUG

spring:
  devtools:
    restart:
      enabled: true
      additional-exclude: static/**
    livereload:
      enabled: false # we use Webpack dev server + BrowserSync for livereload
  jackson:
    serialization:
      indent-output: true
  datasource:
    type: com.zaxxer.hikari.HikariDataSource
    url: jdbc:postgresql://localhost:5432/gearx7
    username: gearx7
    password: gearx7
    hikari:
      poolName: Hikari
      auto-commit: false
  liquibase:
    # Remove 'faker' if you do not want the sample data to be loaded automatically
    contexts: dev, faker
  mail:
    host: localhost
    port: 25
    username:
    password:
  messages:
    cache-duration: PT1S # 1 second, see the ISO 8601 standard
  thymeleaf:
    cache: false

server:
  port: 8080

# ===================================================================
# JHipster specific properties
#
# Full reference is available at: https://www.jhipster.tech/common-application-properties/
# ===================================================================

jhipster:
  # CORS is only enabled by default with the "dev" profile
  cors:
    # Allow Ionic for JHipster by default (* no longer allowed in Spring Boot 2.4+)
    allowed-origins: 'http://localhost:8100,https://localhost:8100,http://localhost:9000,https://localhost:9000,http://localhost:4200,https://localhost:4200'
    # Enable CORS when running in GitHub Codespaces
    allowed-origin-patterns: 'https://*.githubpreview.dev'
    allowed-methods: '*'
    allowed-headers: '*'
    exposed-headers: 'Authorization,Link,X-Total-Count,X-${jhipster.clientApp.name}-alert,X-${jhipster.clientApp.name}-error,X-${jhipster.clientApp.name}-params'
    allow-credentials: true
    max-age: 1800
  security:
    authentication:
      jwt:
        # This token must be encoded using Base64 and be at least 256 bits long (you can type `openssl rand -base64 64` on your command line to generate a 512 bits one)
        base64-secret: MDYwYTU1MjQ1YTNkMGZjYzNhZjM3MzYzZjcwMWJhMDFmYTlhMTU2M2Y2ODFiZDA0ZTExOWJlZjg4NWNlM2I3YmViNjdkZDE5N2M0MGYzYmM5NTliMDY4ZjY3ZDViZjMyMzUxMTFhODdlNjY4MjdjMzY0MDMzODcxNzk0NGU5OTI=
        # Token is valid 24 hours
        token-validity-in-seconds: 86400
        token-validity-in-seconds-for-remember-me: 2592000
  mail: # specific JHipster mail property, for standard properties see MailProperties
    base-url: http://127.0.0.1:8080
  logging:
    use-json-format: false # By default, logs are not in Json format
    logstash: # Forward logs to logstash over a socket, used by LoggingConfiguration
      enabled: false
      host: localhost
      port: 5000
      ring-buffer-size: 512
# ===================================================================
# Application specific properties
# Add your own application properties here, see the ApplicationProperties class
# to have type-safe configuration, like in the JHipsterProperties above
#
# More documentation is available at:
# https://www.jhipster.tech/common-application-properties/
# ===================================================================

# application:

gearx7:
  machine:
    search-radius-km: 15.0

cloudinary:
  cloud_name: dlszistcj
  api_key: 698842318943615
  api_secret: 2P88Es8NTsHiWWUMqN-hoY7RnnY

# this is SMS service congiguration
sms:
  templates:
    booking-created-user: 'Hello %s %s, your booking %s has been created successfully. Machine: %s. From: %s. To: %s. Status: %s. Thanks for choosing Gearx7.'

    booking-created-partner: 'Hello %s %s, your machine has been booked successfully. Booking ID: %s. Customer: %s. Machine: %s. From: %s. To: %s. Status: %s. Thanks for choosing Gearx7.'

    booking-accepted: 'Hello %s %s, your booking %s has been ACCEPTED. Thank you for choosing Gearx7.'

    booking-rejected: 'Hello %s %s, your booking %s has been REJECTED. Please try another machine. Thank you for choosing Gearx7.'

    booking-cancelled-user: 'Hello %s %s, your booking %s has been cancelled successfully. Thank you for choosing Gearx7.'

    booking-cancelled-partner: 'Hello %s %s, booking %s has been cancelled by the customer. Machine: %s. Thank you for choosing Gearx7.'

  msg91:
    auth-key: 165660AijPO1tt8riE596c8eb4
    sender-id: VEHTRK
    booking-user-template-id: 695e081b268710369224fc79

otp:
  msg91:
    auth-key: 165660AijPO1tt8riE596c8eb4
    sender-id: VEHTRK
    otp-template-id: 695e081b268710369224fc79

locationiq:
  api-key: pk.185919f9a8d1c5539f39a4da2b58b488
  url: https://us1.locationiq.com/v1/reverse
